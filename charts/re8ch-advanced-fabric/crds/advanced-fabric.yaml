apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata: {name: advancedfabrics.networking.re8ch.com}
spec:
  group: networking.re8ch.com
  scope: Cluster
  names: {plural: advancedfabrics, singular: advancedfabric, kind: AdvancedFabric, shortNames: [afabric]}
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources: {status: {}}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [mode, nativeRoutingCIDR, acceleratedCIDR, serviceCIDR, nodes]
              properties:
                mode: {type: string, enum: [observe-only, guarded-apply]}
                applyEnabled: {type: boolean}
                emergencyDisable: {type: boolean}
                nativeRoutingCIDR: {type: string}
                acceleratedCIDR: {type: string}
                serviceCIDR: {type: string}
                controlPlaneApi:
                  type: object
                  required: [enabled, vip, port, mode, eligibleNodes, primaryTransport, fallbackTransport, healthCheck]
                  properties:
                    enabled: {type: boolean}
                    vip: {type: string, pattern: '^$|^10\.250\.0\.[0-9]{1,3}/32$'}
                    port: {type: integer, enum: [6443]}
                    mode: {type: string, enum: [healthy-ecmp]}
                    eligibleNodes: {type: array, items: {type: string}}
                    guardedNodes: {type: array, items: {type: string}}
                    nodeOperations: {type: array, items: {type: object, x-kubernetes-preserve-unknown-fields: true}}
                    primaryTransport: {type: string, enum: [wireguard]}
                    fallbackTransport: {type: string, enum: [zerotier]}
                    healthCheck: {type: object, x-kubernetes-preserve-unknown-fields: true}
                observeDays: {type: integer, minimum: 0}
                timezone: {type: string}
                controller: {type: object, x-kubernetes-preserve-unknown-fields: true}
                componentAPI:
                  type: object
                  required: [publishAssessments, validitySeconds]
                  properties:
                    publishAssessments: {type: boolean}
                    validitySeconds: {type: integer, minimum: 30, maximum: 3600}
                networkQuality: {type: object, x-kubernetes-preserve-unknown-fields: true}
                weightedEcmp: {type: object, x-kubernetes-preserve-unknown-fields: true}
                policyRouting: {type: object, x-kubernetes-preserve-unknown-fields: true}
                nodes: {type: array, items: {type: object, x-kubernetes-preserve-unknown-fields: true}}
            status: {type: object, x-kubernetes-preserve-unknown-fields: true}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata: {name: networkinterventions.networking.re8ch.com}
spec:
  group: networking.re8ch.com
  scope: Cluster
  names: {plural: networkinterventions, singular: networkintervention, kind: NetworkIntervention, shortNames: [nint]}
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources: {status: {}}
      additionalPrinterColumns:
        - {name: Subject, type: string, jsonPath: .spec.subjectRef.name}
        - {name: Cause, type: string, jsonPath: .spec.cause}
        - {name: State, type: string, jsonPath: .status.identificationState}
        - {name: Detected, type: date, jsonPath: .spec.detectedAt}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [subjectRef, cause, detectedAt, fingerprint]
              properties:
                subjectRef: {type: object, x-kubernetes-preserve-unknown-fields: true}
                cause: {type: string, enum: [GitOpsGeneration, RoutePolicy, BGPPeer, Gateway, NodeReadiness, Datapath, RIBFIB]}
                detectedAt: {type: string, format: date-time}
                fingerprint: {type: string, minLength: 8}
                changedVariables: {type: array, items: {type: string}}
                evidenceRefs: {type: array, items: {type: string}}
                windows: {type: object, x-kubernetes-preserve-unknown-fields: true}
                controls: {type: object, x-kubernetes-preserve-unknown-fields: true}
            status:
              type: object
              properties:
                identificationState: {type: string, enum: [Discovered, PendingWindows, Eligible, Confounded]}
                confounders: {type: array, items: {type: string}}
                conditions: {type: array, items: {type: object, x-kubernetes-preserve-unknown-fields: true}}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata: {name: networkobservationepisodes.networking.re8ch.com}
spec:
  group: networking.re8ch.com
  scope: Cluster
  names: {plural: networkobservationepisodes, singular: networkobservationepisode, kind: NetworkObservationEpisode, shortNames: [noe]}
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources: {status: {}}
      additionalPrinterColumns:
        - {name: Subject, type: string, jsonPath: .spec.subjectRef.name}
        - {name: Cause, type: string, jsonPath: .spec.cause}
        - {name: State, type: string, jsonPath: .status.state}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [subjectRef, cause, detectedAt, windows]
              properties:
                subjectRef: {type: object, x-kubernetes-preserve-unknown-fields: true}
                cause: {type: string, enum: [NaturalRIBFIBChange, NaturalBGPChange, NaturalPathChange, NaturalReachabilityChange]}
                detectedAt: {type: string, format: date-time}
                collectorEpoch: {type: string, nullable: true}
                baseline: {type: object, x-kubernetes-preserve-unknown-fields: true}
                change: {type: object, x-kubernetes-preserve-unknown-fields: true}
                criteria: {type: object, x-kubernetes-preserve-unknown-fields: true}
                windows: {type: object, x-kubernetes-preserve-unknown-fields: true}
            status:
              type: object
              properties:
                state: {type: string, enum: [Incomplete, Complete, Confounded]}
                startedAt: {type: string, format: date-time}
                endedAt: {type: string, format: date-time, nullable: true}
                measurements: {type: object, x-kubernetes-preserve-unknown-fields: true}
                evidenceRefs: {type: array, items: {type: string}}
                missingEvidence: {type: array, items: {type: string}}
                confounders: {type: array, items: {type: string}}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata: {name: networktradeoffestimates.networking.re8ch.com}
spec:
  group: networking.re8ch.com
  scope: Cluster
  names: {plural: networktradeoffestimates, singular: networktradeoffestimate, kind: NetworkTradeoffEstimate, shortNames: [nte]}
  versions:
    - name: v1alpha1
      served: true
      storage: true
      additionalPrinterColumns:
        - {name: Relation, type: string, jsonPath: .spec.relation}
        - {name: State, type: string, jsonPath: .status.state}
        - {name: Tau, type: number, jsonPath: .status.tau}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [subjectRef, relation, interventionRef]
              properties:
                subjectRef: {type: object, x-kubernetes-preserve-unknown-fields: true}
                relation: {type: string, pattern: '^[QKHCRD]->[QKHCRD]$'}
                interventionRef: {type: string}
                metricQueries: {type: array, items: {type: string}}
            status:
              type: object
              required: [state]
              properties:
                state: {type: string, enum: [Identified, InsufficientEvidence, Confounded]}
                tau: {type: number, nullable: true}
                confidenceInterval: {type: object, x-kubernetes-preserve-unknown-fields: true}
                sampleCount: {type: integer, minimum: 0}
                direction: {type: string, enum: [positive, negative, zero, unknown]}
                windows: {type: object, x-kubernetes-preserve-unknown-fields: true}
                evidenceRefs: {type: array, items: {type: string}}
                missingEvidence: {type: array, items: {type: string}}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata: {name: tradeofftriangles.networking.re8ch.com}
spec:
  group: networking.re8ch.com
  scope: Cluster
  names: {plural: tradeofftriangles, singular: tradeofftriangle, kind: TradeoffTriangle, shortNames: [tt]}
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources: {status: {}}
      additionalPrinterColumns:
        - {name: Vertices, type: string, jsonPath: .spec.vertexLabel}
        - {name: State, type: string, jsonPath: .status.state}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [vertices, edges]
              properties:
                vertexLabel: {type: string}
                vertices:
                  type: array
                  minItems: 3
                  maxItems: 3
                  items: {type: string, enum: [Q, K, H, C, R, D]}
                edges:
                  type: array
                  minItems: 3
                  maxItems: 3
                  items: {type: string, pattern: '^[QKHCRD]->[QKHCRD]$'}
                subjectSelector: {type: object, x-kubernetes-preserve-unknown-fields: true}
                hypothesis: {type: string}
            status:
              type: object
              properties:
                state: {type: string, enum: [Open, Closed]}
                observedVertices: {type: array, items: {type: string}}
                identifiedEdges: {type: array, items: {type: string}}
                missingEvidence: {type: array, items: {type: string}}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata: {name: networkpathassessments.networking.re8ch.com}
spec:
  group: networking.re8ch.com
  scope: Cluster
  names:
    plural: networkpathassessments
    singular: networkpathassessment
    kind: NetworkPathAssessment
    shortNames: [npa]
  versions:
    - name: v1alpha1
      served: true
      storage: false
      subresources: {status: {}}
      additionalPrinterColumns:
        - {name: Subject, type: string, jsonPath: .spec.subjectRef.name}
        - {name: State, type: string, jsonPath: .status.state}
        - {name: Observed, type: date, jsonPath: .status.observedAt}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [subjectRef, scope]
              properties:
                subjectRef:
                  type: object
                  required: [apiVersion, kind, name]
                  properties:
                    apiVersion: {type: string, minLength: 1}
                    kind: {type: string, minLength: 1}
                    namespace: {type: string, minLength: 1}
                    name: {type: string, minLength: 1}
                scope:
                  type: object
                  required: [plane, direction, protocol]
                  properties:
                    plane: {type: string, enum: [host, pod, host-and-pod]}
                    direction: {type: string, enum: [forward, return, bidirectional]}
                    protocol: {type: string, enum: [tcp, udp, mixed]}
            status:
              type: object
              properties:
                observedAt: {type: string, format: date-time}
                validUntil: {type: string, format: date-time}
                state: {type: string, enum: [Ready, Partial, Unknown, Stale, Contradictory]}
                dimensions:
                  type: object
                  properties:
                    optimality: {type: number, minimum: 0, maximum: 1, nullable: true}
                    stability: {type: number, minimum: 0, maximum: 1, nullable: true}
                    independence: {type: number, minimum: 0, maximum: 1, nullable: true}
                confidence:
                  type: object
                  properties:
                    optimality: {type: number, minimum: 0, maximum: 1}
                    stability: {type: number, minimum: 0, maximum: 1}
                    independence: {type: number, minimum: 0, maximum: 1}
                measurements: {type: object, x-kubernetes-preserve-unknown-fields: true}
                calculation: {type: object, x-kubernetes-preserve-unknown-fields: true}
                evidenceRefs:
                  type: array
                  items: {type: string}
                diagnosis: {type: string}
                recommendation: {type: string}
                validation: {type: object, x-kubernetes-preserve-unknown-fields: true}
                conditions:
                  type: array
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys: [type]
                  items:
                    type: object
                    required: [type, status, reason, lastTransitionTime]
                    properties:
                      type: {type: string}
                      status: {type: string, enum: ["True", "False", Unknown]}
                      reason: {type: string}
                      message: {type: string}
                      lastTransitionTime: {type: string, format: date-time}
    - name: v1alpha2
      served: true
      storage: true
      subresources: {status: {}}
      additionalPrinterColumns:
        - {name: Subject, type: string, jsonPath: .spec.subjectRef.name}
        - {name: State, type: string, jsonPath: .status.state}
        - {name: Reachable, type: boolean, jsonPath: .status.pathEvidence.reachable}
        - {name: Observed, type: date, jsonPath: .status.observedAt}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [subjectRef, scope]
              properties:
                subjectRef: {type: object, x-kubernetes-preserve-unknown-fields: true}
                scope: {type: object, x-kubernetes-preserve-unknown-fields: true}
            status:
              type: object
              properties:
                observedAt: {type: string, format: date-time}
                validUntil: {type: string, format: date-time}
                state: {type: string, enum: [Ready, Partial, Unknown, Stale, Contradictory]}
                nodeReady: {type: boolean, nullable: true}
                collectorEpoch: {type: string}
                pathEvidence:
                  type: object
                  properties:
                    reachable: {type: boolean, nullable: true}
                    currentPathMeasured: {type: boolean}
                    viableAlternatives: {type: integer, minimum: 0, nullable: true}
                    freshPlanes: {type: array, items: {type: string}}
                    missingEvidence: {type: array, items: {type: string}}
                evidenceRefs: {type: array, items: {type: string}}
                conditions:
                  type: array
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys: [type]
                  items:
                    type: object
                    required: [type, status, reason, lastTransitionTime]
                    properties:
                      type: {type: string}
                      status: {type: string, enum: ["True", "False", Unknown]}
                      reason: {type: string}
                      message: {type: string}
                      lastTransitionTime: {type: string, format: date-time}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata: {name: trafficpolicies.networking.re8ch.com}
spec:
  group: networking.re8ch.com
  scope: Namespaced
  names: {plural: trafficpolicies, singular: trafficpolicy, kind: TrafficPolicy, shortNames: [tpolicy]}
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources: {status: {}}
      additionalPrinterColumns:
        - {name: Profile, type: string, jsonPath: .spec.profile}
        - {name: Ready, type: string, jsonPath: ".status.conditions[0].status"}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required: [podSelector, destination, profile]
              properties:
                podSelector: {type: object, x-kubernetes-preserve-unknown-fields: true}
                destination:
                  type: object
                  properties:
                    cidrs: {type: array, items: {type: string}}
                    class: {type: string, enum: [cluster, public-internet]}
                  anyOf:
                    - required: [cidrs]
                    - required: [class]
                profile: {type: string, enum: [fastest, balanced, greedy]}
                fallbackProfile: {type: string, enum: [fastest, balanced, greedy], default: balanced}
                priority: {type: integer, minimum: 0, maximum: 100000, default: 100}
            status: {type: object, x-kubernetes-preserve-unknown-fields: true}
